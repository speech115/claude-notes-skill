# Notes Shortlink Site

Tiny Vercel site for `notes.speech115.com`.

- `/install` redirects to the raw `install.sh` in `speech115/notes-skill`
- `/` shows the human-friendly install command

Deploy with:

```bash
vercel deploy /Users/sereja/Projects/products/notes-skill/redirect-site --prod -y
```
